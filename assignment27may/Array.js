const nums = [1, 2, 3];
const doubled = nums.map(n => n * 2);
console.log(doubled); // [2, 4, 6]

const words = ['hello', 'world'];
const caps = words.map(w => w.toUpperCase());
console.log(caps); // ['HELLO', 'WORLD']
